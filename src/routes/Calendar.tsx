import PagePlaceholder from '@/components/PagePlaceholder'

export default function Calendar() {
  return (
    <PagePlaceholder
      title="Calendar"
      phase="Phase 5"
      description="Monthly grid to plan and manage schoolwork & events, plus a yearly overview to see the whole year at a glance."
    />
  )
}
