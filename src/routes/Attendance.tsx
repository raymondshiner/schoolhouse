import PagePlaceholder from '@/components/PagePlaceholder'

export default function Attendance() {
  return (
    <PagePlaceholder
      title="Attendance"
      phase="Phase 2"
      description="A fast daily flow to mark the whole family present, absent, half-day, field trip, or holiday — plus a mandated-days counter."
    />
  )
}
