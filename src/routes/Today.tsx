import PagePlaceholder from '@/components/PagePlaceholder'

export default function Today() {
  return (
    <PagePlaceholder
      title="Today"
      phase="Phase 2"
      description="Your family's day at a glance — attendance status, what's next in each loop, and today's events."
    />
  )
}
