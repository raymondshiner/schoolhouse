# Session state

## Where we left off

Built the **entire Cycle 1 feature set** for Schoolhouse in one session (solo, no
parallel Smiths — judged cold uncoordinated agents too collision-prone on shared
files without a human referee).

**Done & pushed to `main`:**
- Phase 0: Supabase Google SSO auth, protected routing, React Query, dark mode,
  responsive shell (desktop sidebar + mobile bottom nav), PWA, dev seed.
- Phase 1 Kids, Phase 2 Attendance (+ mandated-days counter), Phase 3 Loop
  (multi-loop, up-next, mark-done advances pointer), Phase 4 Hours & Credits
  (courses + progress bars), Phase 5 Calendar (month grid + year heatmap),
  Phase 6 Books, plus a Today dashboard.
- Route-level code-splitting; `verify-ui.mjs` (6/6 on the auth gate, desktop +
  mobile). `npm run build` and `tsc -b` both clean.

**Immediate next steps:**
1. **Log in and smoke-test the signed-in app** — I could NOT runtime-verify the
   authenticated screens headlessly (Google SSO isn't automatable). Build +
   typecheck pass and the code is coherent, but real-data rendering of Kids /
   Attendance / Loop / Hours / Calendar / Books is unverified. Sign in at
   http://localhost:5173, then run `supabase/seed.sql` to load a sample family.
2. **Deploy to Vercel** — import the repo, add `VITE_SUPABASE_URL` +
   `VITE_SUPABASE_ANON_KEY` env vars, then add the Vercel URL to Supabase Auth
   redirect URLs + the Google OAuth client.
3. If anything looks off in the signed-in UI, that's the first fix queue.

**Open questions (in PROJECT.md):** field-trip double-counting (attendance status
vs calendar event), and authenticated-flow E2E testing strategy (needs a seeded
Supabase session).

**Backend note:** repo is linked to Supabase via GitHub integration; migrations
in `supabase/migrations/` deploy on push. `supabase/seed.sql` is manual.
